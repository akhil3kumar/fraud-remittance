spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/akhil3kumar/fraud-remittance.git
          search-paths: configuration-repository
          clone-on-start: true

server:
  port: 8888
